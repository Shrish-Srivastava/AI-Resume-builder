export function ProofPage() {
  return (
    <div style={{ padding: 'var(--space-4)', maxWidth: 'var(--max-text-width)', margin: '0 auto' }}>
      <h1 style={{ fontFamily: 'var(--font-serif)', fontSize: 'var(--text-headline-sm)', fontWeight: 600, marginBottom: 'var(--space-2)', color: 'var(--color-text)' }}>
        Proof
      </h1>
      <p style={{ color: 'var(--color-text-muted)', lineHeight: 'var(--line-height-loose)' }}>
        Placeholder for artifacts. This section will be built in a future step.
      </p>
    </div>
  );
}
